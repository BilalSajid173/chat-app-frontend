import { Fragment } from "react";
import SignupForm from "../components/AuthForm/Signup"

const Signup = (props) => {
  return (
    <Fragment>
      <SignupForm />
    </Fragment>
  );
};

export default Signup;