import { Fragment } from "react";
import LoginForm from "../components/AuthForm/Login";

const Login = (props) => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default Login;
